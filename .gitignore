# This repo has no build. It ships CSS, two woff2 files, and documentation.
# Keep this file short — if it starts growing, something is being generated
# here that probably should not be.

node_modules/
.DS_Store
*.log
.env
.env.*
!.env.example

# Claude Code: settings.json and rules/ are shared and committed; this one is per-machine.
.claude/settings.local.json
